# Not used by the script directly; kept for reference.
# You will need:
# - client_secret.json : OAuth client credentials downloaded from Google Cloud Console
# - token.json         : Created automatically on first auth
